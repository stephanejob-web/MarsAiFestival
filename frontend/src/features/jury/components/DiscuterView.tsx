import React from "react";

const DiscuterView = (): React.JSX.Element => {
    return (
        <div className="flex flex-1 items-center justify-center text-sm text-mist">
            Discuter — coming soon
        </div>
    );
};

export default DiscuterView;
