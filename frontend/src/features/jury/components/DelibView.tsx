import React from "react";

const DelibView = (): React.JSX.Element => {
    return (
        <div className="flex flex-1 items-center justify-center text-sm text-mist">
            Délibération — coming soon
        </div>
    );
};

export default DelibView;
