import React from "react";

const ListesView = (): React.JSX.Element => {
    return (
        <div className="flex flex-1 items-center justify-center text-sm text-mist">
            Listes — coming soon
        </div>
    );
};

export default ListesView;
